# mrs-test-repo-corrupted

A repository with an intentionally corrupted MATLAB test file (missing end keyword).
Used for QE negative testing (TC-11): validates that MRT reports errors when test files have syntax errors.
